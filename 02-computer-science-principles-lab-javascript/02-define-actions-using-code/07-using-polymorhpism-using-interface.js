/**
 * SECTION:
 * Using Polymorphism
 */

/**
 * EXERCISE:
 * 1. Create an interface called `Shape` with a method called `area`.
 */

// Exercise 1
// Tasks:
// 1. Create an interface called `Shape` with a method called `area`.

// Exclusive to TypeScript Only
// interface Shape {
//     area(): number;
// }