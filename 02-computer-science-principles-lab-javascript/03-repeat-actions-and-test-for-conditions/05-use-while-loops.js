// Create a variable
let count = 0;

// While Loop
while (count < 100) {
    
    // All numbers divisible by 9
    if (count % 9 == 0) {
        console.log(count); // If true, log the count
    }

    count++; // Increment the count
}

// Do While Loop
let otherCount = 0;
do {
    console.log(otherCount); // Log the otherCount
    otherCount++; // Increment the otherCount
}
while (otherCount < 10); // Check if otherCount is less than 10